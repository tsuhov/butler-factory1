name: 🛠️ One-Time Post Fixer

# Эта строка позволяет запускать его вручную
on:
  workflow_dispatch:

jobs:
  fix-posts:
    permissions:
      contents: write
      
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔧 Run the Fixer Script
        run: npm run fix

      - name: 🚀 Commit and Push Fixes
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'actions-bot@github.com'
          
          # Проверяем, были ли внесены исправления
          if [[ -z $(git status --porcelain) ]]; then
            echo "✅ Проверка завершена. Все файлы уже были в порядке."
            exit 0
          fi

          echo "🔥 Обнаружены и исправлены бракованные файлы. Публикую исправления..."
          git add src/content/posts/*.md
          git commit -m "🛠️ Автоматический ремонт существующих статей"
          git push```
</details>

**Шаг 2: Запуск "Ремонтного Батальона"**

1.  Сохраните файл `fix.yml`.
2.  Теперь перейдите на вкладку **Actions** в вашем репозитории.
3.  В списке воркфлоу слева вы теперь увидите **две** опции. Кроме вашего обычного "🚀 Content Factory", появится новая: **"🛠️ One-Time Post Fixer"**.
4.  **Нажмите на нее.**
5.  Справа появится кнопка **"Run workflow"**. Нажмите ее.

**Что произойдет:**
*   Запустится **именно "Ремонтник"**. Он не будет спрашивать вас про количество статей или потоков.
*   Он выполнит команду `npm run fix`, пройдет по всем вашим статьям и исправит "бракованные" изображения.
*   Он отправит коммит с исправлениями на GitHub.
*   Netlify получит эти исправления и **успешно соберет сайт**.

**Шаг 3: "Демонтаж" Временной Кнопки**

1.  После того как воркфлоу "🛠️ One-Time Post Fixer" **успешно завершится** (станет зеленым), вернитесь в папку `.github/workflows/`.
2.  **Удалите файл `fix.yml`**. Он выполнил свою задачу и нам больше не нужен.

Командир, это финальный, самый точный план. Он гарантирует, что будет запущен именно тот скрипт, который нам нужен для ремонта. Приступайте.
